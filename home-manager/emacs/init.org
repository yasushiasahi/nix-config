#+title: My Configuration for GNU Emacs
#+AUTHOR: Yasushi Asahi
#+EMAIL: asahi1600@gmail.com
#+STARTUP: content

* 最初に行うべき
** ヘッダー
#+begin_src emacs-lisp :tangle yes
;;; package --- 初期設定ファイル -*- lexical-binding: t; -*-
;;; Commentary:
;;; Emacsの全体的な設定やパッケージ管理を行います。
;;; Code:
#+end_src
** package.ei 標準パッケージマネージャー
外部パッケージのソースはすべてnixで入れているのでミニマムでやる気ない設定。
[[https://qiita.com/nobuyuki86/items/e392b642189d755dd113#packageel][このような]]凝った設定もある。「built-inパッケージも更新対象にする」が気になる。
#+begin_src emacs-lisp :tangle yes
  (require 'package)
  (package-initialize)
  (add-to-list 'package-archives '("melpa" . "https://melpa.org/packages/"))
#+end_src
** [[https://github.com/purcell/exec-path-from-shell][exec-path-from-shell]] Shellの環境変数をEmacsに同期する
#+begin_src emacs-lisp :tangle yes
  (use-package exec-path-from-shell
    :ensure t

    :custom
    (exec-path-from-shell-shell-name "/run/current-system/sw/bin/fish")
    ;; デフォルは"-i"。シェルをインタラクティブに起動すると時間かかるので引数なしにする。
    ;; https://github.com/purcell/exec-path-from-shell?tab=readme-ov-file#making-exec-path-from-shell-faster
    (exec-path-from-shell-arguments nil)
    ;; どの環境変数を取り込むか
    (exec-path-from-shell-variables '("PATH" "TYPE_SCRIPT_LIB"))

    :config
    (exec-path-from-shell-initialize))
#+end_src

* Emacs組み込み機能の設定、有効化
** customize set variable結果の置き場所指定
tangleした後のinit.elなら書き込んでくれても良いが。nix/store/ファイルに書き込むことはおそらくできない。
#+begin_src emacs-lisp :tangle yes
(setopt custom-file (locate-user-emacs-file "custom.el"))
#+end_src
** バックアップファイル（自動生成ファイル）
- 自動保存リストファイル
  - 自動保存ファイル(auto-save-file)のリスト？
  - 8年Emacsを使ってきて見ようと思ったことがないので不要
#+begin_src emacs-lisp :tangle yes
  ;; 編集中のファイルを定期的に保存してバックアップとして残す（自動保存ファイル）。-> ~/.config/emacs/buckup/
  (setopt auto-save-file-name-transforms `((".*" ,(locate-user-emacs-file "backup/") t)))

  ;; ファイルオープン時(編集前)のファイルをバックアップとして残す。-> ~/.config/emacs/buckup/
  (setopt backup-directory-alist `((".*" . ,(locate-user-emacs-file "backup"))))

  ;; 自動保存リストファイルは作成しない
  (setopt auto-save-list-file-prefix nil)

  ;; 予めバックアップファイルを納めるディレクトリを作っておく
  (make-directory (locate-user-emacs-file "backup") t)
#+end_src
** ロックファイル
- ロックファイル
  - 共同編集中のファイルの更新競合を防ぐのが重な目的
  - Emacsでファイルを共同編集することはないので不要
#+begin_src emacs-lisp :tangle yes
  ;; ロックファイルは作成しない
  (setopt create-lockfiles nil)
#+end_src
** ビルトインの設定
#+begin_src emacs-lisp :tangle yes
  ;; タブの幅。各言語のモードのタブはばは別でそのモードの設定でそれぞれ設定しないといけない。
  (setopt tab-width 2)

  ;; yesと打たずにyだけで答える
  (defalias 'yes-or-no-p 'y-or-n-p)

  ;; 警告音（ピープ音）をならさい
  (setopt ring-bell-function 'ignore)

  ;; read-onlyなテキストをkilしようとしても警告を表示しない
  (setopt kill-read-only-ok t)

  ;; 行頭でC-k(kill-line)したときにその行自体を消す
  (setopt kill-whole-line nil)

  ;; タブ文字を使用しない
  ;; ちなみに、untabifyでバッファ無いの全てのタブをスペースに置き換えられる。tabifyはその逆。
  (setq-default indent-tabs-mode nil)

  ;; 行を折り返さない
  (setopt truncate-lines t)

  ;; 画面がスクロールする時にカーソルを画面上の位置で固定する
  (setopt scroll-preserve-screen-position t)

  ;; 1行ずつスクロールする
  (setopt scroll-conservatively 1)
#+end_src
** キーバインド
| C-h | バックスペース           |
| M-h | 単語単位のバックスペース |
| C-d | デリート                 |
| M-d | 単語単位のデリート       |
#+begin_src emacs-lisp :tangle yes
  ;; https://www.emacswiki.org/emacs/BackspaceKey
  (define-key key-translation-map [?\C-?] [?\C-h])
  (define-key key-translation-map [?\M-\d] [?\M-h])
  (define-key key-translation-map [?\C-h] [?\C-?])
  (define-key key-translation-map [?\M-h] [?\M-\d])
#+end_src

| C-a | 行頭、文頭に行き来する |
#+begin_src emacs-lisp :tangle yes
  (defun my/beginning-of-line-text-or-line ()
    "行の最初の文字の位置に移動。すでに最初の文字だったら行頭に移動。"
    (interactive)
    (let ((curr-point (point))            ; コマンド実行前のカーソル位置
          (curr-column (current-column))) ; コマンド実行前の行番号
      (back-to-indentation)               ; 一旦行の最初の文字の位置に移動
      (when (and (/= curr-column 0)       ; 元々行頭にいなかった
                 (<= curr-point (point))) ; 最初の文字の位置よりも前にいた
        (beginning-of-line))))            ; その場合は行頭に移動

  (define-key global-map (kbd "C-a") #'my/beginning-of-line-text-or-line)
#+end_src

| C-g | 汎用キャンセル |
#+begin_src emacs-lisp :tangle yes
  ;; 関数名とコメント以外はそのまま使わせていただいている
  ;; https://protesilaos.com/codelog/2024-11-28-basic-emacs-configuration/#h:83c8afc4-2359-4ebe-8b5c-f2e5257bdda3
  (defun my/keyboard-quit-dwim ()
    "一般的な `keyboard-quit' のための「意図を汲んだ」動作。

  汎用的な `keyboard-quit' は、ミニバッファが開いているときに
  期待される動作をしません。私たちが望むのは、明示的にフォーカスを
  当てなくても、ミニバッファを閉じることです。

  このコマンドのDWIM動作は以下の通りです：

  - リージョンがアクティブな場合、それを無効化する。
  - ミニバッファが開いているがフォーカスされていない場合、ミニバッファを閉じる。
  - Completionsバッファが選択されている場合、それを閉じる。
  - その他すべての場合は、通常の `keyboard-quit' を使用する。"
    (interactive)
    (cond
     ((region-active-p)
      (keyboard-quit))
     ((derived-mode-p 'completion-list-mode)
      (delete-completion-window))
     ((> (minibuffer-depth) 0)
      (abort-recursive-edit))
     (t
      (keyboard-quit))))

  (define-key global-map (kbd "C-g") #'my/keyboard-quit-dwim)
#+end_src
** スタートアップメッセージ（ [[https://github.com/tkf/emacs-request][request]] HTTPクライアント）
#+begin_src emacs-lisp :tangle yes
  (use-package request :ensure t)

  (defun display-startup-echo-area-message ()
    "スタートアップメッセージとして名言を表示する"
    (request "https://meigen.doodlenote.net/api/json.php"
      :parser 'json-read
      :success (cl-function
                (lambda (&key data &allow-other-keys)
                  (let* ((item (aref data 0))
                         (meigen (alist-get 'meigen item))
                         (auther (alist-get 'auther item)))
                    (message "%s\nby %s" meigen auther))))))
#+end_src
#+begin_src emacs-lisp :tangle no
  ;; 表示を高速化しようと頑張った残骸
  ;; (setq my/startup-msg nil)
  ;;
  ;; (request "https://meigen.doodlenote.net/api/json.php"
  ;;   :parser 'json-read
  ;;   :success (cl-function
  ;;             (lambda (&key data &allow-other-keys)
  ;;               (let* ((item (aref data 0))
  ;;                      (meigen (alist-get 'meigen item))
  ;;                      (auther (alist-get 'auther item)))
  ;;                 (setq my/startup-msg (format "%s\nby %s" meigen auther))))))
  ;;
  ;; (defun display-startup-echo-area-message ()
  ;;   "スタートアップメッセージとして名言を表示する"
  ;;   (let ((count 0))
  ;;     (while (and (not my/startup-msg) (< count 6))
  ;;       (sleep-for 0.25)
  ;;       (setq count (1+ count)))
  ;;     (message my/startup-msg)))
#+end_src
** recentf - 開いたファイルの履歴を保存
#+begin_src emacs-lisp :tangle yes
  (use-package recentf
    :hook ((emacs-startup . recentf-mode))

    :config
    ;; 履歴の最大数
    (setopt recentf-max-saved-items 1024)

    ;; 履歴に含めたくないファイル
    (add-to-list 'recentf-exclude "~/.config/")
    (add-to-list 'recentf-exclude "/nix/store/")
    (add-to-list 'recentf-exclude "/node_modules/"))
#+end_src
** savehist - 変数の状態を永続化
emacsを閉じてもミニバッファ補完履歴、コード補完履歴、kill-ringの状態を保存しておく。
[[https://emacsredux.com/blog/2026/04/07/stealing-from-the-best-emacs-configs/#persist-the-kill-ring-across-sessions-doom-prot][kill-ringは軽くしてから保存する]]
#+begin_src emacs-lisp :tangle yes
  (use-package savehist
    :ensure nil

    :init
    (setopt savehist-save-minibuffer-history t)                       ; ミニバッファの履歴を保存
    (setopt savehist-additional-variables '(kill-ring corfu-history)) ; 永続化する変数を指定

    (savehist-mode)

    :hook
    (savehist-save . my/unpropertize-kill-ring)

    :config
    (defun my/unpropertize-kill-ring ()
      "kill-ringから文字列以外のfaceなどプロパティを削ぎ落とす"
      (setq kill-ring
            (mapcar #'substring-no-properties
                    (cl-remove-if-not #'stringp kill-ring)))))
#+end_src
** delsel - 選択状態で入力した時に選択箇所を削除する
#+begin_src emacs-lisp :tangle yes
  (use-package delsel
    :config
    (delete-selection-mode))
#+end_src
** autorevert - emacs外で表示中のfileが更新された時にbufferを再描画する
#+begin_src emacs-lisp :tangle yes
  (use-package autorevert
    :config
    ;; https://github.com/stevemolitor/claude-code.el?tab=readme-ov-file#tips-and-tricks
    (setopt auto-revert-use-notify nil)

    (global-auto-revert-mode 1))
#+end_src
** face-remap - 一時的なフォントサイズを変更
普段は限界までフォントサイズを小さくしているが、画面共有などで他人にEmacsを見てもらう時に便利。
以前までは、C-<wheel-up>, C-<wheel-down>で変更していたけど、これだとバッファローカルになる。
グローバルに変更するにはvim-jpで見かけた以下をやる。
mouse-wheel-global-text-scale。 C-M-<wheel-up>, C-M-<wheel-down> でもサイズ変更できる。
#+begin_src emacs-lisp :tangle yes
  (use-package face-remap
    :bind (("C-x M-=" . global-text-scale-adjust)
           ("C-x M-0" . global-text-scale-adjust)
           ("C-x M-+" . global-text-scale-adjust)
           ("C-x M--" . global-text-scale-adjust))

    :config
    (setopt text-scale-mode-step 2))
#+end_src
** project - プロジェクト管理
#+begin_src emacs-lisp :tangle yes
  (use-package project
    :config
    ;; 指定したファイルがあるディレクトリをプロジェクトのルートとして扱う
    (setopt project-vc-extra-root-markers '(".dir-locals-2.el" ".dir-locals.el" ".envrc")))
#+end_src

* 見た目
** [[https://github.com/bbatsov/solarized-emacs][solarized-theme]] 定番のカラーテーマ
#+begin_src emacs-lisp :tangle yes
  (use-package solarized-theme
    :ensure t

    :custom
    (solarized-use-less-bold t)
    (x-underline-at-descent-line t)

    :init
    (load-theme 'solarized-dark t))
#+end_src
** [[https://github.com/eki3z/mini-echo.el][mini-echo]] モードラインを非表示にして、echoエリアで最低限の情報表示をする
#+begin_src emacs-lisp :tangle yes
  (use-package mini-echo
    :ensure t

    :config
    (mini-echo-mode))
#+end_src
** [[https://github.com/protesilaos/pulsar][pulsar]] バッファが書き換わった場所を強調表示する
#+begin_src emacs-lisp :tangle yes
  (use-package pulsar
    :ensure t

    :init
    (pulsar-global-mode 1)

    :config
    (add-hook 'minibuffer-setup-hook #'pulsar-pulse-line)
    (add-hook 'consult-after-jump-hook #'pulsar-recenter-top)
    (add-hook 'consult-after-jump-hook #'pulsar-reveal-entry)
    (add-hook 'imenu-after-jump-hook #'pulsar-recenter-top)
    (add-hook 'imenu-after-jump-hook #'pulsar-reveal-entry))
#+end_src
** [[https://github.com/rainstormstudio/nerd-icons.el?tab=readme-ov-file#installing-fonts][nerd-icons]] Nerdアイコンを扱いやすくする
#+begin_src emacs-lisp :tangle yes
  (use-package nerd-icons
    :ensure t

    :custom
    ;; デフォルトの設定が"Symbols Nerd Font Mono"。
    ;; nix-darwinでインストールしないといけないので明示的書いている。
    ;; https://github.com/rainstormstudio/nerd-icons.el?tab=readme-ov-file#installing-fonts
    (nerd-icons-font-family "Symbols Nerd Font Mono"))
#+end_src
** [[https://github.com/rainstormstudio/nerd-icons-dired][nerd-icons-dired]] DiredにNerdアイコンを表示する
#+begin_src emacs-lisp :tangle yes
  (use-package nerd-icons-dired
    :ensure t

    :hook
    (dired-mode . nerd-icons-dired-mode))
#+end_src
** [[https://github.com/mickeynp/ligature.el][ligature]] Maple Monoフォントのリガチャを設定する
#+begin_src emacs-lisp :tangle yes
  (use-package ligature
    :ensure t

    :config
    ;; 書き方の参考
    ;; https://github.com/mickeynp/ligature.el/wiki
    ;; 対象のリガチャされる文字列。"0xA12 0x56 1920x1080"と"\\ \\' \\."は多分書き方間違ってる。
    ;; https://github.com/subframe7536/maple-font/blob/variable/source/features/README.md#ligatures
    (ligature-set-ligatures 'prog-mode
                            '("::"      "?."                    "<#--"
                              ":::"     "..<"                   "<!---->"
                              "?:"      ".="                    "<->"
                              ":?"      "<~"                    "<-->"
                              ":?>"     "~>"                    "->"
                              "<:"      "~~"                    "<-"
                              ":>"      "<~>"                   "-->"
                              ":<"      "<~~"                   "<--"
                              "<:<"     "~~>"                   ">->"
                              ">:>"     "-~"                    "<-<"
                              "__"      "~-"                    "|->"
                              "#{"      "~@"                    "<-|"
                              "#["      "~~~~~~~"               "-------"
                              "#("      "0xA12 0x56 1920x1080"  ">--"
                              "#?"      "<>"                    "--<"
                              "#!"      "</"                    "<|||"
                              "#:"      "/>"                    "|||>"
                              "#="      "</>"                   "<||"
                              "#_"      "<+"                    "||>"
                              "#__"     "+>"                    "<|"
                              "#_("     "<+>"                   "|>"
                              "]#"      "<*"                    "<|>"
                              "#######" "*>"                    "_|_"
                              "<<"      "<*>"                   "[TRACE]"
                              "<<<"     ">="                    "[DEBUG]"
                              ">>"      "<="                    "[INFO]"
                              ">>>"     "<=<"                   "[WARN]"
                              "{{"      "==>"                   "[ERROR]"
                              "}}"      "=="                    "[FATAL]"
                              "{|"      "==="                   "[TODO]"
                              "|}"      "!="                    "[FIXME]"
                              "{{--"    "!=="                   "[NOTE]"
                              "{{!--"   "=/="                   "[HACK]"
                              "--}}"    "=!="                   "[MARK]"
                              "[|"      "|="                    "[EROR]"
                              "|]"      "<=>"                   "[WARNING]"
                              "!!"      "<==>"                  "todo))"
                              "||"      "<=="                   "fixme))"
                              "??"      "==>"                   "Cl"
                              "???"     "=>"                    "al"
                              "&&"      "<=|"                   "cl"
                              "&&&"     "|=>"                   "el"
                              "//"      "=<="                   "il"
                              "///"     "=>="                   "tl"
                              "/*"      "======="               "ul"
                              "/**"     ">=<"                   "xl"
                              "*/"      ":="                    "ff"
                              "++"      "=:"                    "tt"
                              "+++"     ":=:"                   "all"
                              ";;"      "=:="                   "ell"
                              ";;;"     "\\ \\' \\."            "ill"
                              ".."      "--"                    "ull"
                              "..."     "---"                   "ll"
                              ".?"      "<!--"))

    (global-ligature-mode t))

#+end_src

* ユーティリティー
** [[https://codeberg.org/ideasman42/emacs-undo-fu][undo-fu]] Undo/Redoの改善
何やら[[https://qiita.com/nobuyuki86/items/e392b642189d755dd113#undo-%E3%82%A2%E3%83%B3%E3%83%89%E3%82%A5%E9%96%A2%E9%80%A3][Emacs組み込みのUndoが改善されたらしい]]ので一旦向こうか
#+begin_src emacs-lisp :tangle yes
  (use-package undo-fu
    :ensure t

    :config
    ;; Emacsのデフォルトのクリップボード履歴の容量が小さすぎるらしく、大きくしている。
    ;; https://codeberg.org/ideasman42/emacs-undo-fu#undo-limits
    (setq undo-limit 67108864) ; 64mb.
    (setq undo-strong-limit 100663296) ; 96mb.
    (setq undo-outer-limit 1006632960) ; 960mb.

    (global-set-key (kbd "C-/")   'undo-fu-only-undo)
    (global-set-key (kbd "C-?") 'undo-fu-only-redo))
#+end_src
** [[https://github.com/casouri/vundo][vando]] キルリングのツリー操作
#+begin_src emacs-lisp :tangle yes
  (use-package vundo
    :ensure t

    :custom
    ;; ツリーの見た目が少しだけ可愛くなる
    (vundo-glyph-alist vundo-unicode-symbols)

    :bind ("C-M-/" . vundo))
#+end_src
** [[https://github.com/Wilfred/helpful][Helpful]] より見や すい*help*
#+begin_src emacs-lisp :tangle yes
  (use-package helpful
    :ensure t

    :bind (([remap describe-function] . helpful-callable)
           ([remap describe-variable] . helpful-variable)
           ([remap describe-key]      . helpful-key)
           ([remap describe-command] . helpful-command)
           ([remap Info-goto-emacs-command-node] . helpful-function)
           ("C-c C-d" . helpful-at-point)))
#+end_src
*** describe-*した時*HELP*バッファのウィンドウにフォーカスする。
こうしておくとqですぐに閉じられる。
この設定をまではわざわざ*HELP*のウィンドウまで移動しないと閉じられなくて、気軽にdescribe-*できなかった。
#+begin_src emacs-lisp :tangle yes
  (use-package help
    :config
    (setopt help-window-select t))
#+end_src
** ウィンドウ操作を直感的に
#+begin_src emacs-lisp :tangle yes
  (use-package window
    :after magit

    :config
    (transient-define-prefix my/window-operation ()
      "Window Operation"
      :transient-suffix     'transient--do-stay
      :transient-non-suffix 'transient--do-exit
      [:class transient-columns
              ["Move"
               ("p" "↑" windmove-up)
               ("n" "↓" windmove-down)
               ("b" "←" windmove-left)
               ("f" "→" windmove-right)]
              ["Ajust"
               ("<up>" "↑" shrink-window)
               ("<down>" "↓" enlarge-window)
               ("<left>" "←" shrink-window-horizontally)
               ("<right>" "→" enlarge-window-horizontally)]
              ["Split"
               ("\\" "vertical" split-window-right)
               ("-" "horizontal" split-window-below)
               ("s" "swap" window-swap-states)
               ("e" "balance" balance-windows)]
              ["Ohter"
               ("0" "delete" delete-window)
               ("1" "only" delete-other-windows)
               ("t" "maxmaiz" toggle-frame-maximized)]])

    (put 'my/window-operation 'interactive-only nil)

    (define-key global-map (kbd "C-t") #'my/window-operation)
    )
#+end_src
** which-key キーバインドの候補表示
#+begin_src emacs-lisp :tangle yes
  (use-package which-key
    :ensure t

    :config
    (which-key-mode))
#+end_src
** [[http://github.com/szermatt/visual-replace][visual-replace]] 置換インターフェイス
#+begin_src emacs-lisp :tangle yes
  (use-package visual-replace
    :ensure t

    :config
    (visual-replace-global-mode 1))
#+end_src
** 括弧
*** 対応する括弧を自動挿入
#+begin_src emacs-lisp :tangle yes
  (use-package elec-pair
    :config
    (electric-pair-mode))
#+end_src
*** [[https://github.com/AmaiKinono/puni][puni]] 括弧を賢く扱う
#+begin_src emacs-lisp :tangle yes
  (use-package puni
    :ensure t
    :defer t

    :hook ((prog-mode eval-expression-minibuffer-setup) . puni-mode))
#+end_src
** eldoc
*** [[https://github.com/casouri/eldoc-box][eldoc-box]] eldocの内容をchild frameに表示する
#+begin_src emacs-lisp :tangle yes
  (use-package eldoc-box
    :ensure t

    :bind (("C-c d" . eldoc-box-help-at-point))

    :custom
    (eldoc-box-clear-with-C-g t)
    (eldoc-box-max-pixel-width 1200)
    (eldoc-box-max-pixel-height 1050)

    :config
    ;; lsp-proxyを使っている分にはこの設定不要かもしれない
    (add-hook 'eldoc-box-buffer-setup-hook #'eldoc-box-prettify-ts-errors 0 t))
#+end_src
** [[https://github.com/Fanael/rainbow-delimiters][rainbow-delimiters]] - 対応する括弧ごとに色をつける
#+begin_src emacs-lisp :tangle yes
  (use-package rainbow-delimiters
    :ensure t

    :hook (prog-mode . rainbow-delimiters-mode))
#+end_src
** [[https://github.com/abo-abo/avy][avy]] - 劇的にカーソル移動を早くする
[[https://emacs-jp.github.io/tips/avy-can-do-anything][Avyならなんでもできる]]
#+begin_src emacs-lisp :tangle yes
  (use-package avy
    :ensure t

    :bind (("C-s" . avy-goto-char-timer))

    :custom
    (avy-background t))
#+end_src
** [[https://github.com/akicho8/string-inflection][string-inflection]] - Caseをいい感じに操作する
#+begin_src emacs-lisp :tangle yes
  (use-package string-inflection
    :ensure t
    :after magit

    :bind ("M-i" . my/string-inflection-operation)

    :config
    (transient-define-prefix my/string-inflection-operation ()
      "Window Operation"
      :transient-suffix 'transient--do-exit
      [:class transient-columns
              ["Single word"
               ("v" "EMACS" upcase-word)
               ("a" "emacs" downcase-word)
               ("." "Emacs" capitalize-word)]
              ["Mulchple Words"
               ("c" "fooBar" string-inflection-lower-camelcase)
               ("C" "FooBar" string-inflection-camel-case)
               ("s" "foo_bar" string-inflection-snake-case)
               ("S" "FOO_BAR" string-inflection-upcase)
               ("p" "Foo_Bar" string-inflection-capital-snake-case)
               ("k" "foo-bar" string-inflection-kebab-case)]
              ["Cycle"
               ("j" "cycle" string-inflection-java-style-cycle)
               ("r" "cycle" string-inflection-ruby-style-cycle)]]))
#+end_src
** [[http://github.com/joaotavora/yasnippet][yasnippet]] 略語展開
#+begin_src emacs-lisp :tangle yes
  (use-package yasnippet
    :ensure t

    :config
    (yas-global-mode 1))

  (use-package yasnippet-snippets
    :ensure t)
#+end_src
** flymake シンタックスチェッカー
#+begin_src emacs-lisp :tangle no
  (use-package flymake
    :hook (prog-mode conf-mode))
#+end_src
** [[https://github.com/fritzgrabo/flymake-cspell][flymake-cspell]] flymakeのCSpell統合
#+begin_src emacs-lisp :tangle yes
  (use-package flymake-cspell
    :ensure
    :commands flymake-cspell-setup)
#+end_src
** [[https://github.com/magnars/multiple-cursors.el][multiple-cursors]] 矩形編集
#+begin_src emacs-lisp :tangle yes
  (use-package multiple-cursors
    :ensure t

    :bind (("C->" . mc/mark-next-like-this)
           ("C-<" . mc/mark-previous-like-this)
           ("C-c C-<" . mc/mark-all-like-this))
    )
#+end_src
** grepの結果を編集する [[https://github.com/mhayashi1120/Emacs-wgrep][wgrep]]
#+begin_src emacs-lisp :tangle no
  (use-package wgrep
    :ensure t)
#+end_src
** [[https://github.com/emacsorphanage/macrostep][macrostep]] use-packageなどのマクロを展開してデバッグする
#+begin_src emacs-lisp :tangle yes
  (use-package macrostep
    :ensure t

    :commands (macrostep-expand macrostep-mode))
#+end_src
* ミニバッファ補完
** [[https://github.com/minad/vertico][vertico]] - ミニバッファ補完UI
#+begin_src emacs-lisp :tangle yes
  (use-package vertico
    :ensure t

    :init
    ;; minibufferの最大高
    (setopt vertico-count 30)

    (vertico-mode))

  (use-package emacs
    :ensure nil

    :init
    ;; ミニバッファ
    (setopt enable-recursive-minibuffers t) ; ミニバッファ内で新たにミニバッファを開けるように
    (setopt minibuffer-prompt-properties '( ; M-x とか Find file: とかミニバッファの左端に表示される文字の設定
                                           read-only t             ; 編集不可
                                           cursor-intangible t     ; カーソルが侵入させない
                                           face minibuffer-prompt  ; 使用するface
                                           ))

    ;; M-xで表示される候補を現在のモードで実行可能なコマンドだけに限定
    (setopt read-extended-command-predicate #'command-completion-default-include-p))
#+end_src

補完UIの表示形式バリエーションを増やす
| =M-B= | バッファに表示       |
| =M-F= | 水平方向             |
| =M-G= | グリッド             |
| =M-R= | プロンプトが下になる |
| =M-U= | 最上位の候補のみ表示 |
| =M-V= | 通常                 |
#+begin_src emacs-lisp :tangle yes
  (use-package vertico-multiform
    :ensure nil

    :init
    ;; バッファに表示する時はframeの右半分に表示
    (setopt vertico-buffer-display-action '(display-buffer-in-direction
                                            (direction . right)))


    (setopt vertico-multiform-categories
            '((file (:keymap . vertico-directory-map))))

    (vertico-multiform-mode))
#+end_src

マウスでスクロール&選択できるようにする
#+begin_src emacs-lisp :tangle yes
  (use-package vertico-mouse
    :ensure nil

    :init
    (vertico-mouse-mode))
#+end_src

直前に実行したミニバッファコマンドを再度呼び出せるようにする
#+begin_src emacs-lisp :tangle yes
  (use-package vertico-repeat
    :ensure nil

    :bind ("M-R" . vertico-repeat)

    :hook (minibuffer-setup . vertico-repeat-save))
#+end_src
** [[https://github.com/oantolin/orderless][orderless]] - 補完スタイル
スペース区切りによる柔軟で直感的、現代的なマッチが可能
「補完スタイル」についての神記事 https://qiita.com/keita44_f4/items/12a4a7081b0092eaca94
partial-completionは ~/p/t/f~ が ~/path/to/file~ にマッチするスタイル
#+begin_src emacs-lisp :tangle yes
  (use-package orderless
    :ensure t

    :init
    ;; 補完スタイルのリスト。リストの先頭の補完スタイルから試し、補完候補が0個だったら次の補完スタイルにフォールバックする
    (setopt completion-styles '(orderless basic))

    ;; カテゴリ（file,command,bufferとか）に応じてcompletion-stylesを切り替える
    (setopt completion-category-overrides '((file (styles orderless partial-completion))))
    (setopt completion-category-defaults nil)

    ;; partial-completionスタイルの挙動を変える。
    ;; t  : 先頭一致 foo -> foo.*
    ;; nil: 部分一致 foo -> .*foo.*
    (setopt completion-pcm-leading-wildcard t))
#+end_src
** [[https://github.com/minad/marginalia][marginalia]] - 補完候補の横に注釈を表示
#+begin_src emacs-lisp :tangle yes
  (use-package marginalia
    :ensure t

    :init
    (marginalia-mode)

    :bind (:map minibuffer-local-map
                ("M-A" . marginalia-cycle)))
#+end_src
** [[https://github.com/rainstormstudio/nerd-icons-completion][nerd-icons-completion]] - アイコン表示
#+begin_src emacs-lisp :tangle yes
  (use-package nerd-icons-completion
    :ensure t

    :init
    (nerd-icons-completion-mode)

    :hook
    (marginalia-mode . nerd-icons-completion-marginalia-setup))
#+end_src
** [[https://github.com/minad/consult][consult]] - 検索&ナビゲーションコマンド集
#+begin_src emacs-lisp :tangle yes
  (use-package consult
    :ensure t

    :init
    (setopt xref-show-xrefs-function #'consult-xref)
    (setopt xref-show-definitions-function #'consult-xref)
    (setopt consult-preview-key (list :debounce 0.5 'any))

    :bind (;; C-c bindings in `mode-specific-map'
           ("C-c M-x" . consult-mode-command)
           ;; C-x bindings in `ctl-x-map'
           ("C-x b" . consult-buffer)
           ("C-x M-p" . consult-project-buffer)
           ("C-x C-g" . my/consult-ghq-magit-status)
           ("s-m" . my/consult-ghq-ghostel)
           ;; Other custom bindings
           ("M-y" . consult-yank-pop)
           ;; M-g bindings in `goto-map'
           ("M-g f" . consult-flymake)
           ("M-g g" . consult-goto-line)
           ("M-g m" . consult-mark)
           ("M-g k" . consult-global-mark)
           ("M-g i" . consult-imenu)
           ("M-g I" . consult-imenu-multi)
           ("M-g o" . consult-outline)
           ;; M-s bindings in `search-map'
           ("M-s d" . consult-fd)
           ("M-s g d" . my-consult-ghq-fd)
           ("M-s c" . consult-locate)
           ("M-s r" . consult-ripgrep)
           ("M-s g r" . my-consult-ghq-ripgrep)
           ("M-s l" . my/consult-line)
           ("M-s L" . consult-line-multi))

    :config
    (defun my/consult-line (flag)
      "prefix付きで呼び出された場合、カーソル位置のシンボルをデフォルトの検索文字列とする"
      (interactive "P")
      (if flag
          (consult-line (thing-at-point 'symbol))
        (consult-line)))

    (defun my-consult-ghq--list-candidates ()
      "ghq listの結果をリストで返す"
      (with-temp-buffer
        (unless (zerop (apply #'call-process "ghq" nil t nil '("list" "--full-path")))
          (error "Failed: Cannot get ghq list candidates"))
        (let ((paths))
          (goto-char (point-min))
          (while (not (eobp))
            (push (buffer-substring-no-properties
                   (line-beginning-position)
                   (line-end-position))
                  paths)
            (forward-line 1))
          (nreverse paths))))
    (defun my-consult-ghq--read ()
      "ghq管理のリポジトリ一覧から選ぶ"
      (consult--read (my-consult-ghq--list-candidates)
                     :state (consult--file-preview)
                     :prompt "ghq: "
                     :category 'file))

    (defun my-consult-ghq-fd ()
      "ghq管理のリポジトリ一覧から選び、プロジェクト内ファイル検索"
      (interactive)
      (consult-fd (my-consult-ghq--read)))

    (defun my-consult-ghq-ripgrep ()
      "ghq管理のリポジトリ一覧から選び、プロジェクト内でripgrep"
      (interactive)
      (consult-ripgrep (my-consult-ghq--read)))

    (defun my/consult-ghq-magit-status ()
      "ghq管理のリポジトリ一覧から選び、magit statusを開く"
      (interactive)
      (magit-status (my-consult-ghq--read)))

    (defun my/consult-ghq-ghostel ()
      "ghq管理のリポジトリ一覧から選び、ghostelを開く"
      (interactive)
      (let ((default-directory (my-consult-ghq--read)))
        (ghostel)))

    (defun my-consult-switch-buffer-kill ()
      "Kill candidate buffer at point within the minibuffer completion."
      (interactive)
      ;; The vertico--candidate has a irregular char at the end.
      (let ((name  (substring (vertico--candidate) 0 -1)))
        (when (bufferp (get-buffer name))
          (kill-buffer name))))
    )
#+end_src
** [[https://github.com/mohkale/consult-yasnippet][consult-yasnippet]] - yasnippet連携
#+begin_src emacs-lisp :tangle yes
  (use-package consult-yasnippet
    :ensure t

    :bind (("C-x M-s" . consult-yasnippet)))
#+end_src
** [[https://github.com/oantolin/embark][embark]] - 補完候補に対してのアクション実行
一般的なGUIにおける右クリックみたいなもの。
~embark-act~ でカーソル下にある候補、シンボルに応じたアクション候補（fileならrenameとかdeleteとか。URLならブラウザで開くとか。）から選んで実行する。
~prefix-help-command~: プレフィックスキー(C-xとかC-c)の途中でC-hを押した際(e.g. C-x C-h)に実行するコマンド定義。デフォルトでは*help*バッファにプレフィックス配下のキーバインド一覧を表示する。
#+begin_src emacs-lisp :tangle yes
  (use-package embark
    :ensure t

    :init
    ;; プレフィックス配下のキーバインドをミニバッファで一覧して選択できる
    (setopt prefix-help-command #'embark-prefix-help-command)

    :bind
    ("C-." . embark-act)         ; メインの機能。アクション候補をミニバッファに開く
    ("C-;" . embark-dwim)        ; アクション候補の中から選択するステップを省略して一番それっぽいアクションを実行する
    ("C-h B" . embark-bindings)  ; カレントバッファのキーバインド一覧表示
    )
#+end_src

consultインテグレーション
#+begin_src emacs-lisp :tangle yes
  (use-package embark-consult
    :ensure t)
#+end_src
* コード補完
** [[https://github.com/minad/corfu][corfu]] - コード補完UI
#+begin_src emacs-lisp :tangle yes
  (use-package corfu
    :ensure t

    :init
    ;; 自動実行
    (setopt corfu-auto t)                 ; 自動実行を有効化
    (setopt corfu-auto-prefix 1)          ; capf（補完）が発火する文字数
    (setopt corfu-auto-delay 0.0)         ; {corfu-auto-prefix}文字入力してからcapfを発火させるまでの秒数

    (global-corfu-mode))
#+end_src

補完候補のドキュメント表示
corfuに同梱されてる
#+begin_src emacs-lisp :tangle yes
  (use-package corfu-popupinfo
    :ensure nil

    :init
    ;; 候補を選択してからドキュメントが表示されるまでのデバウンス時間
    (setopt corfu-popupinfo-delay '(2.0 . 0.5)) ; １つ目はcapf後の初回表示。二つ目は初回表示後お別候補選択。

    ;; 表示エリアのサイズ
    (setopt corfu-popupinfo-max-width 120)      ; 最大幅
    (setopt corfu-popupinfo-max-height 120)     ; 最大高

    (corfu-popupinfo-mode))
#+end_src

補完の選択履歴に基づいて候補をソートする
corfuに同梱されてる
#+begin_src emacs-lisp :tangle yes
  (use-package corfu-history
    :ensure nil

    :init
    (corfu-history-mode))
#+end_src
** [[https://github.com/minad/cape][cape]] - completion-at-point-functionsの拡張
基本的にlsp-proxyが有効になるbufferでの作業が主なので、add-hookしている機能を使うことは殆どないはずだけど、みんな設定しているで現在お試し中。org-modeでは役に立ちそう。
"C-c p"から直接cape-*を実行するのは結構便利かもしれない。
#+begin_src emacs-lisp :tangle yes
  (use-package cape
    :ensure t

    :init
    ;; バッファ内の単語
    (add-hook 'completion-at-point-functions #'cape-dabbrev)
    ;; カレントディレクトリからのパス
    (add-hook 'completion-at-point-functions #'cape-file)
    ;; プログラミング言語のキーワードconstとかifとか
    (add-hook 'completion-at-point-functions #'cape-keyword)
    ;; org-modeのelispブロック内の単語
    (add-hook 'completion-at-point-functions #'cape-elisp-block)

    :bind ("C-c p" . cape-prefix-map))
#+end_src
** [[https://github.com/axelf4/hotfuzz][hotfuzz]] flexスタイルの高速ソートアルゴリズム
[[https://github.com/axelf4/emacs-completion-bench][こちらのベンチマーク]]によるとかなり早い。
cで書かれた動的モジュールが別途必要だがnix経由だと勝手に入る。 (featurep 'hotfuzz-module)でモジュールが読み込まれているか確認できる。
[[https://github.com/minad/corfu#buffer-localcorfu-only-completion-styles][corfuのREADME]]に書かれてるやり方でcorfuのみ適用する。
#+begin_src emacs-lisp :tangle yes
  (use-package hotfuzz
    :ensure t

    :hook ((corfu-mode . (lambda ()
                           (setq-local completion-styles '(hotfuzz)
                                       completion-category-overrides nil
                                       completion-category-defaults nil)))))
#+end_src
** [[https://github.com/LuigiPiucco/nerd-icons-corfu][nerd-icons-corfu]] - アイコンを表示
READMEの通りに設定
#+begin_src emacs-lisp :tangle yes
  (use-package nerd-icons-corfu
    :ensure t

    :init
    (add-to-list 'corfu-margin-formatters #'nerd-icons-corfu-formatter))
#+end_src
* git
** [[https://magit.vc/][magit]] gitクライアント
#+begin_src emacs-lisp :tangle yes
  (use-package magit
    :ensure t

    :config
    (require 'magit-extras)
    (require 'transient))
#+end_src
** [[https://github.com/dgutov/diff-hl][diff-hl]] 行頭にgitのステータスを表示する
#+begin_src emacs-lisp :tangle yes
  (use-package diff-hl
    :ensure t

    :hook ((magit-post-refresh . diff-hl-magit-post-refresh)
           (dired-mode . diff-hl-dired-mode))

    :config
    (global-diff-hl-mode)
    (global-diff-hl-show-hunk-mouse-mode))
#+end_src
** [[https://github.com/sshaw/git-link][git-link]] 現在開いているブランチ/ファイル/行のgithubのを作る
#+begin_src emacs-lisp :tangle yes
  (use-package git-link
    :ensure t)
#+end_src
** [[https://github.com/redguardtoo/vc-msg][vc-msg]] 現在行の直近のコミットを表示する
#+begin_src emacs-lisp :tangle yes
  (use-package vc-msg
    :ensure t)
#+end_src
** [[https://github.com/dandavison/magit-delta][magit-delta]] diffを見やすくする
#+begin_src emacs-lisp :tangle yes
  (use-package magit-delta
    :ensure t

    :hook ((magit-mode . magit-delta-mode))

    :custom
    (magit-delta-default-dark-theme "Solarized (dark)"))
#+end_src

* マイナーモード
** [[https://github.com/casouri/expreg][expreg]] リージョンを段階的に広げる
現在のポイントを中心にリージョン(選択範囲)を広げていく。
個人的Emacsのキラープラグインの一つ。コピペエンジニアとしてはこれがないとまともに編集できない。
#+begin_src emacs-lisp :tangle yes
  (use-package expreg
    :ensure t

    :bind (("C-z" . expreg-expand)
           ("C-M-z" . expreg-contract)))
#+end_src
** [[https://github.com/magnars/tagedit][tagedit]] htmlタグを扱う
#+begin_src emacs-lisp :tangle yes
  (use-package tagedit
    :ensure t

    :bind (("C-c k" . tagedit-kill)))
#+end_src
** [[https://github.com/purcell/envrc][envrc.el]] direnvインテグレーション
#+begin_src emacs-lisp :tangle yes
  (use-package envrc
    :ensure t

    :hook (after-init . envrc-global-mode))
#+end_src
** [[https://github.com/smihica/emmet-mode][emmet-mode]] emmetインテグレーション
#+begin_src emacs-lisp :tangle yes
  (use-package emmet-mode
    :ensure t

    :hook ((tsx-ts-mode
            astro-ts-mode
            html-ts-mode
            css-ts-mode) . emmet-mode)

    :config
    (add-to-list 'emmet-jsx-major-modes 'jtsx-tsx-mode))
#+end_src
** treesit シンタックスハイライト
#+begin_src emacs-lisp :tangle yes
  (use-package treesit
    :custom
    (treesit-font-lock-level 4))
#+end_src

* 言語
** [[https://github.com/Malabarba/aggressive-indent-mode][aggressive-indent]] Emacs Lisp
#+begin_src emacs-lisp :tangle yes
  (use-package aggressive-indent
    :ensure t

    :hook ((emacs-lisp-mode . aggressive-indent-mode))

    :config
    (add-to-list 'aggressive-indent-excluded-modes 'lisp-interaction-mode))
#+end_src
** JavaSctipt
#+begin_src emacs-lisp :tangle yes
  (use-package js-ts-mode
    :custom
    (js-indent-level 2)

    :mode ("\\.js\\'" "\\.cjs\\'" "\\.mjs\\'"))
#+end_src
** TypeScript
#+begin_src emacs-lisp :tangle yes
  (use-package typescript-ts-mode
    :mode ("\\.ts\\'" "\\.mts\\'" "\\.cts\\'"))
#+end_src
** [[https://github.com/llemaitre19/jtsx][jtsx]] JSX / TSX
#+begin_src emacs-lisp :tangle yes
  (use-package jtsx
    :ensure t

    :mode (("\\.jsx\\'" . jtsx-jsx-mode)
           ("\\.tsx\\'" . jtsx-tsx-mode))

    :custom
    (js-indent-level 2)

    :config
    (defun jtsx-bind-keys-to-mode-map (mode-map)
      "Bind keys to MODE-MAP."
      (define-key mode-map (kbd "C-c C-j") 'jtsx-jump-jsx-element-tag-dwim)
      (define-key mode-map (kbd "C-c j o") 'jtsx-jump-jsx-opening-tag)
      (define-key mode-map (kbd "C-c j c") 'jtsx-jump-jsx-closing-tag)
      (define-key mode-map (kbd "C-c j r") 'jtsx-rename-jsx-element)
      (define-key mode-map (kbd "C-c <down>") 'jtsx-move-jsx-element-tag-forward)
      (define-key mode-map (kbd "C-c <up>") 'jtsx-move-jsx-element-tag-backward)
      (define-key mode-map (kbd "C-c C-<down>") 'jtsx-move-jsx-element-forward)
      (define-key mode-map (kbd "C-c C-<up>") 'jtsx-move-jsx-element-backward)
      (define-key mode-map (kbd "C-c C-S-<down>") 'jtsx-move-jsx-element-step-in-forward)
      (define-key mode-map (kbd "C-c C-S-<up>") 'jtsx-move-jsx-element-step-in-backward)
      (define-key mode-map (kbd "C-c j w") 'jtsx-wrap-in-jsx-element)
      (define-key mode-map (kbd "C-c j u") 'jtsx-unwrap-jsx)
      (define-key mode-map (kbd "C-c j d n") 'jtsx-delete-jsx-node)
      (define-key mode-map (kbd "C-c j d a") 'jtsx-delete-jsx-attribute)
      (define-key mode-map (kbd "C-c j t") 'jtsx-toggle-jsx-attributes-orientation)
      (define-key mode-map (kbd "C-c j h") 'jtsx-rearrange-jsx-attributes-horizontally)
      (define-key mode-map (kbd "C-c j v") 'jtsx-rearrange-jsx-attributes-vertically))

    (defun jtsx-bind-keys-to-jtsx-jsx-mode-map ()
      (jtsx-bind-keys-to-mode-map jtsx-jsx-mode-map))

    (defun jtsx-bind-keys-to-jtsx-tsx-mode-map ()
      (jtsx-bind-keys-to-mode-map jtsx-tsx-mode-map))

    (add-hook 'jtsx-jsx-mode-hook 'jtsx-bind-keys-to-jtsx-jsx-mode-map)
    (add-hook 'jtsx-tsx-mode-hook 'jtsx-bind-keys-to-jtsx-tsx-mode-map))
#+end_src
** [[https://github.com/Sorixelle/astro-ts-mode][astro-ts-mode]] Astro
#+begin_src emacs-lisp :tangle yes
  (use-package astro-ts-mode
    :ensure t

    :mode "\\.astro\\'")
#+end_src

astroのgrammarがなければインストール。本当はnixで入れたいけどやり方がよく分からん。
#+begin_src emacs-lisp :tangle no
  (unless (treesit-language-available-p 'astro nil)
    (let ((treesit-language-source-alist  '((astro "https://github.com/virchau13/tree-sitter-astro"))))
      (treesit-install-language-grammar 'astro)))
#+end_src
** HTML
#+begin_src emacs-lisp :tangle yes
  (use-package html-ts-mode
    :mode ("\\.html\\'"))
#+end_src
** CSS
#+begin_src emacs-lisp :tangle yes
  ;; 編集機能としてはscssもcss-ts-modeで十分なのだが、
  ;; apheleiaでPrettierをかける時にcssパーサーを使用することになり
  ;; scssだとフォーマットできないため、別名のモードを作って
  ;; フォーマットオプションを分岐させる。
  (define-derived-mode scss-ts-mode css-ts-mode "SCSS")

  (use-package css-mode
    :mode (("\\.css\\'" . css-ts-mode)
           ("\\.scss\\'" . scss-ts-mode))

    :custom
    (css-indent-offset 2))
#+end_src
** Rust
#+begin_src emacs-lisp :tangle yes
  (use-package rust-ts-mode
    :mode (("\\.rs\\'" . rust-ts-mode))
    :hook ((rust-ts-mode . (lambda ()
                             (setq-local flymake-diagnostic-functions '(lsp-proxy-diagnostics--flymake-backend)))))
    )
#+end_src
** [[https://github.com/nix-community/nix-ts-mode][nix-ts-mode]] nix-ts-mode
#+begin_src emacs-lisp :tangle yes
  (use-package nix-ts-mode
    :ensure t

    :mode "\\.nix\\'")
#+end_src
** [[https://github.com/hcl-emacs/terraform-mode][terraform-mode]] Terraform
#+begin_src emacs-lisp :tangle yes
  (use-package terraform-mode
    :ensure t

    :config
    (add-hook 'terraform-mode-hook #'outline-minor-mode))
#+end_src
** JSON
#+begin_src emacs-lisp :tangle yes
  (use-package json-ts-mode
    :mode ("\\.jsonc?\\'"))
#+end_src
** Python
python.el内でadd-to-listされているので設定いらなさそう。未検証。
#+begin_src emacs-lisp :tangle yes
  (use-package python
    :mode ("\\.py[iw]?\\'" . python-ts-mode)
    :interpreter "python[0-9.]*")
#+end_src
** YAML
yaml-ts-mode.el内でadd-to-listされているので設定いらなさそう。未検証。
#+begin_src emacs-lisp :tangle yes
  (use-package yaml-ts-mode
    :mode ("\\.ya?ml\\'"))
#+end_src
** TOML
toml-ts-mode.elでadd-to-listされているので設定いらなさそう。未検証。
#+begin_src emacs-lisp :tangle yes
  (use-package toml-ts-mode
    :mode ("\\.toml\\'"))
#+end_src
** Dockerfile
dockerfile-ts-mode.elでadd-to-listされているので設定いらなさそう。未検証。
#+begin_src emacs-lisp :tangle yes
  (use-package dockerfile-ts-mode
    :mode ("\\(?:Dockerfile\\(?:\\..*\\)?\\|\\.[Dd]ockerfile\\)\\'"))
#+end_src
** [[https://github.com/jrblevin/markdown-mode][markdown-mode]] Markdown
#+begin_src emacs-lisp :tangle yes
  (use-package markdown-mode
    :ensure t

    :mode ("README\\.md\\'" . gfm-mode)

    :bind (:map markdown-mode-map
                ("C-c C-e" . markdown-do))

    :custom
    (markdown-command "multimarkdown"))
#+end_src
** [[https://github.com/preetpalS/emacs-dotenv-mode][dotenv-mode]]
#+begin_src emacs-lisp :tangle yes
  (use-package dotenv-mode
    :ensure t

    :mode ("\\.env\\..*\\'" . dotenv-mode))
#+end_src
* LSP
** [[https://github.com/jadestrong/lsp-proxy][lsp-proxy]] Rust製 LSPクランアント
#+begin_src emacs-lisp :tangle yes
  (use-package lsp-proxy
    :ensure t

    :init
    ;; corfuのwikiにあるeglot用の設定を参考にしている。
    ;; https://github.com/minad/corfu/wiki#configuring-corfu-for-eglot
    (advice-add 'lsp-proxy-completion-at-point :around #'cape-wrap-buster)

    :custom
    (lsp-proxy-log-buffer-max nil)
    (lsp-proxy-diagnostics-provider :flymake)

    :hook ((tsx-ts-mode
            js-ts-mode
            typescript-ts-mode
            astro-ts-mode
            rust-ts-mode
            nix-ts-mode
            html-ts-mode
            css-ts-mode
            toml-ts-mode
            yaml-ts-mode
            terraform-mode
            json-ts-mode)
           . lsp-proxy-mode)

    :bind (:map lsp-proxy-mode-map
                ("M-." . lsp-proxy-find-definition)
                ("M-," . xref-go-back)

                :prefix-map my/lsp-prefix-map
                :prefix "M-l"
                ("r" . my/lsp-proxy-rename)
                ("f i" . lsp-proxy-find-implementations)
                ("f r" . lsp-proxy-find-references)
                ("e a" . lsp-proxy-execute-code-action)
                ("e c" . lsp-proxy-execute-command)
                ("z" . lsp-proxy-restart)
                ("d" . lsp-proxy-describe-thing-at-point)
                ("s" . lsp-proxy-signature-activate))

    :config
    (defun my/lsp-proxy-rename (newname)
      "lsp-proxy-renameをデフォルト値付きで呼び出す"
      (interactive
       (let ((currnt (or (thing-at-point 'symbol t)
                         "unknown symbol")))
         (list (read-from-minibuffer
                (eglot--format "Rename `%s' to: " currnt)
                currnt
                nil nil nil
                (symbol-name (symbol-at-point))))))
      (lsp-proxy-rename newname))
    )
#+end_src

* 言語ユーティリティー
** [[https://github.com/radian-software/apheleia][apheleia]] いろんな言語のフォーマッター
#+begin_src emacs-lisp :tangle yes
  (use-package apheleia
    :ensure t

    :config
    (setopt apheleia-formatters-respect-indent-level nil)

    ;; Astro
    (add-to-list 'apheleia-formatters '(prettier-astro . ("apheleia-npx" "prettier" "--stdin-filepath" filepath
                                                          "--plugin=prettier-plugin-astro" "--parser=astro")))
    (add-to-list 'apheleia-mode-alist '(astro-ts-mode . prettier-astro))

    ;; Rust
    (add-to-list 'apheleia-formatters '(rustfmt . ("rustfmt" "--edition=2024" "--quiet" "--emit=stdout")))

    ;; Sass
    (add-to-list 'apheleia-mode-alist '(scss-ts-mode . prettier-scss))

    ;; biome
    ;; デフォルトはformatなので、check版を定義
    (add-to-list 'apheleia-formatters '(biome-check . ("apheleia-npx" "biome" "check" "--write" "--stdin-file-path" filepath)))

    ;;biomeを優先して使う
    (add-to-list 'apheleia-mode-alist '(js-ts-mode . biome-check))
    (add-to-list 'apheleia-mode-alist '(typescript-ts-mode . biome-check))
    (add-to-list 'apheleia-mode-alist '(tsx-ts-mode . biome-check))
    (add-to-list 'apheleia-mode-alist '(json-ts-mode . biome-check))
    (add-to-list 'apheleia-mode-alist '(html-ts-mode . biome-check))
    (add-to-list 'apheleia-mode-alist '(css-ts-mode . biome-check))
    (add-to-list 'apheleia-mode-alist '(astro-ts-mode . biome-check))

    ;; package.jsonにpettierがあればpretterにする
    (defun my/set-prettier-formatter ()
      (if (my/check-prettier-in-project)
          (setq-local apheleia-formatter '(prettier))))

    (add-hook 'js-ts-mode-hook #'my/set-prettier-formatter)
    (add-hook 'typescript-ts-mode-hook #'my/set-prettier-formatter)
    (add-hook 'tsx-ts-mode-hook #'my/set-prettier-formatter)
    (add-hook 'json-ts-mode-hook #'my/set-prettier-formatter)
    (add-hook 'html-ts-mode-hook #'my/set-prettier-formatter)
    (add-hook 'css-ts-mode-hook #'my/set-prettier-formatter)

    (defun my/set-prettier-astro-formatter ()
      (if (my/check-prettier-in-project)
          (setq-local apheleia-formatter '(prettier-astro))))

    (add-hook 'astro-ts-mode-hook #'my/set-prettier-astro-formatter)

    (apheleia-global-mode +1))


  (require 'json)

  (defun my/find-package-json (&optional dir)
    "DIR（省略時は `default-directory'）から上方向に遡って package.json を探す。
    見つかれば絶対パスを返し、見つからなければ nil を返す。"
    (let* ((start-dir (or dir default-directory))
           (found-dir (locate-dominating-file start-dir "package.json")))
      (when found-dir
        (expand-file-name "package.json" found-dir))))

  (defun my/read-json-file (path)
    "PATH の JSON ファイルをハッシュテーブルとして読み込む。"
    (if (fboundp 'json-parse-file)
        (json-parse-file path
                         :object-type 'hash-table
                         :array-type 'list
                         :null-object nil
                         :false-object nil)
      (let ((json-object-type 'hash-table)
            (json-array-type 'list)
            (json-key-type 'string))
        (json-read-file path))))

  (defun my/package-json-has-prettier-p (package-json-path)
    "PACKAGE-JSON-PATH に prettier が依存として含まれているかを判定する。
    dependencies と devDependencies の両方をチェックし、t または nil を返す。"
    (and package-json-path
         (file-readable-p package-json-path)
         (let ((data (my/read-json-file package-json-path)))
           (or (when-let ((deps (gethash "dependencies" data)))
                 (and (gethash "prettier" deps) t))
               (when-let ((dev-deps (gethash "devDependencies" data)))
                 (and (gethash "prettier" dev-deps) t))))))

  (defun my/check-prettier-in-project (&optional dir)
    "DIR から上方向に package.json を探し、prettier が依存に含まれているかを返す。
    含まれていれば t、含まれていない（または package.json が見つからない）場合は nil。"
    (let ((package-json (my/find-package-json dir)))
      (and package-json
           (my/package-json-has-prettier-p package-json))))
#+end_src

* Org-mode
** org-mode
#+begin_src emacs-lisp :tangle yes
  (use-package org
    :ensure t

    :init
    ;; .orgファイルを格納するデフォルトディレクトリ
    ;; captureもagendaも使ってないから意味ないかも
    (setopt org-directory "~/ghq/github.com/yasushiasahi/org")

    ;; カテゴリごとのM-RETの挙動。
    ;; t  : カーソル位置で行を分割
    ;; nil: ブロック直下に同じレベルのブロック作る
    (setopt org-M-RET-may-split-line '((headline . t)
                                       (item . t)
                                       (table . t)
                                       (default . t)))

    ;; 少ないタイプ数でブロックを移動
    (setopt org-use-speed-commands t)
    ;; 常にインデントする
    (setopt org-startup-indented t))
#+end_src
** [[https://github.com/org-roam/org-roam][org-roam]] 第二の脳
- complation-at-pointでlinkを挿入できるらしいけどうまくいかない
#+begin_src emacs-lisp :tangle yes
  (use-package org-roam
    :ensure t

    :bind (:map org-mode-map
                ("C-M-i" . completion-at-point)
                :prefix-map my/org-roam-map
                :prefix "C-c n"
                ("l" . org-roam-buffer-toggle)
                ("f" . org-roam-node-find)
                ("i" . org-roam-node-insert))

    :custom
    (org-roam-directory "~/ghq/github.com/yasushiasahi/org/roam")
    (org-roam-complete-everywhere t)

    :config
    (org-roam-setup))
#+end_src
** [[https://github.com/minad/org-modern/tree/main][org-modern]] かっこいい見た目にする
#+begin_src emacs-lisp :tangle yes
  (use-package org-modern
    :ensure t

    :hook ((org-mode . org-modern-mode)
           (org-agenda-finalize . org-modern-agenda))

    :custom
    (org-modern-star 'replace)
    (org-auto-align-tags nil)
    (org-tags-column 0)
    (org-catch-invisible-edits 'show-and-error)
    (org-special-ctrl-a/e t)
    (org-insert-heading-respect-content t)
    (org-hide-emphasis-markers t)
    (org-pretty-entities t)
    (org-agenda-tags-column 0))
#+end_src
** [[https://github.com/jdtsmith/org-modern-indent][org-modern-indent ]] org-modernをindentに対応させる
#+begin_src emacs-lisp :tangle yes
  (use-package org-modern-indent
    :ensure t

    :hook ((org-modern-mode . org-modern-indent-mode))

    :config
    (set-face-attribute 'org-modern-symbol nil :family "Iosevka")
    (set-face-attribute 'org-modern-indent-bracket-line nil :family "Iosevka")
    (set-face-attribute 'org-modern-block-name nil :family "Iosevka")
    (set-face-attribute 'org-block-begin-line nil :underline nil :family "Iosevka")
    (set-face-attribute 'org-block-end-line nil :overline nil :family "Iosevka")
    (set-face-attribute 'org-block nil :background "#001e27"))
#+end_src

* ターミナル
** [[https://github.com/dakra/ghostel][ghostel]] Ghostty in Emacs
#+begin_src emacs-lisp :tangle yes
  (use-package ghostel
    :ensure t
    :defer t

    :bind
    ("C-c g" . ghostel)

    :custom
    (ghostel-shell "/run/current-system/sw/bin/fish")
    (ghostel-keymap-exceptions '("C-x" "C-u" "M-x" "C-c" "M-s" "C-t"))
    (ghostel-buffer-name "*👻*")
    (ghostel-buffer-name-function #'my/ghostel-buffer-name-by-directory)

    :init
    (defun my/ghostel-buffer-name-by-directory (_title)
      "バッファ名を決める。オリジナルとほぼ同じ"
      (format "*👻: %s*"
              (abbreviate-file-name (directory-file-name default-directory)))))
#+end_src
** [[https://github.com/karthink/popper][popper]] バッファをポップアップ表示する
#+begin_src emacs-lisp :tangle yes
  (use-package popper
    :ensure t

    :bind
    ("s-g" . popper-toggle)
    ("s-M-g" . popper-cycle)

    :custom
    (popper-reference-buffers '("^\\*👻.*\\*$" ghostel-mode))
    (popper-mode-line nil)

    :init
    (popper-mode +1)
    (popper-echo-mode +1))
#+end_src

* 自作関数
** Chromeをリロードする
#+begin_src emacs-lisp :tangle yes
  (defun my/reload-chrome ()
    "Chromeをリロードする"
    (interactive)
    (shell-command "osascript -e 'tell application \"Google Chrome\" to reload active tab of front window'"))

  (global-set-key (kbd "s-r") 'my/reload-chrome)

#+end_src

* ユースケース
** 特定のバッファーのポイントを記録してどこからでもその位置にジャンプする
- =C-x r SPC r=(point-to-register) ポイントの記録
- =C-x r j r=(jump-to-register) 記録したポイントへジャンプ
https://ayatakesi.github.io/emacs/25.1/Position-Registers.html#Position-Registers

* Footer
#+begin_src emacs-lisp :tangle yes
  (provide 'init)

  ;;; init.el ends here
#+end_src
